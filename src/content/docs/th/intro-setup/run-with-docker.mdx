---
title: 'รัน GrowthBook ด้วย Docker'
description: 'ติดตั้ง GrowthBook และ MongoDB บนเครื่องของคุณด้วยไฟล์ Docker Compose ไฟล์เดียว แล้วเรียนรู้ทุก port และทุกคำสั่งที่คุณต้องใช้'
sidebar:
  order: 3
---

import RunGrowthBook from '../../../../components/RunGrowthBook.astro';
import CopyCode from '../../../../components/CopyCode.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

วิธีที่เร็วที่สุดในการรัน GrowthBook บนเครื่องของคุณคือใช้ Docker Compose สแตก Compose ด้านล่างนี้จะเริ่ม GrowthBook และ MongoDB ขึ้นมาพร้อมกัน — ไม่ต้องเชื่อมต่อ database เองเลย

<RunGrowthBook />

## ขั้นตอนทีละขั้น

1. **บันทึก compose.yaml** ที่แสดงในกล่องด้านบนไว้ที่ root ของโปรเจกต์คุณ ไฟล์นี้นิยาม service สองตัว — `mongo` และ `growthbook` — และ named volume สองตัว: หนึ่งตัวสำหรับ data directory ของ MongoDB อีกตัวสำหรับเก็บไฟล์ที่อัปโหลด

2. **เริ่ม GrowthBook แบบทำงานเบื้องหลัง** โดยรัน:

```bash
docker compose up -d
```

   flag `-d` (detached mode) จะคืนการควบคุมกลับมาที่ terminal ของคุณทันที โดยทั้งสอง container จะเริ่มทำงานอยู่เบื้องหลัง

3. **เปิด browser ไปที่ `http://localhost:3000`** — นี่คือ GrowthBook web UI ที่คุณใช้สร้าง feature flags, รัน experiments และจัดการบัญชีของคุณ

4. **SDK ของคุณจะสื่อสารกับ `http://localhost:3100`** — นี่คือ endpoint ของ GrowthBook SDK API แอปของคุณจะดึง payload ของ feature flag จาก URL นี้ ไม่ใช่จาก port 3000

5. **หยุดและตรวจสอบสแตก** เมื่อคุณทำเสร็จแล้ว:

```bash
# Stop all containers (data is preserved in the named volumes)
docker compose down

# Tail live logs for the growthbook container only
docker compose logs -f growthbook
```

## compose.yaml ฉบับเต็มสำหรับอ้างอิง

```yaml
services:
  mongo:
    image: mongo:latest
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=password
    volumes: ["mongodata:/data/db"]
  growthbook:
    image: growthbook/growthbook:latest
    ports: ["3000:3000", "3100:3100"]
    depends_on: [mongo]
    environment:
      - MONGO_URL=mongodb://root:password@mongo:27017/
    volumes: ["uploads:/usr/local/src/app/packages/back-end/uploads"]
volumes:
  uploads:
  mongodata:
```

<Callout title="ตรวจสอบแล้วด้วย docker compose">
ทดสอบเมื่อ 2026-09-24 ด้วย `mongo:latest` และ `growthbook/growthbook:latest`: ถ้าไม่มี volume `mongodata`, การรัน `docker compose down` ตามด้วย `docker compose up -d` จะสร้าง MongoDB ใหม่ที่ว่างเปล่า — flag, experiment และ user ทั้งหมดที่คุณสร้างไว้จะหายไป ถึงแม้ volume `uploads` จะยังอยู่ก็ตาม volume `mongodata` ด้านบนจำเป็นเพื่อให้ `docker compose down` ปลอดภัย
</Callout>

## ตารางอ้างอิง Port

| Port | Service | จุดประสงค์ |
|------|---------|---------|
| 3000 | GrowthBook web app | Admin UI — สร้าง flags, experiments, metrics |
| 3100 | GrowthBook SDK API | แอปของคุณดึง feature flags จากที่นี่ |
| 27017 | MongoDB (ภายใน) | ไม่ได้ expose ออกสู่ host; ใช้โดย container ของ GrowthBook เท่านั้น |

## เกี่ยวกับ MongoDB

GrowthBook เก็บ configuration ของตัวเองทั้งหมดไว้ใน MongoDB — นิยามของ feature flag, config ของ experiment, metrics และการตั้งค่าของทีม MongoDB **ไม่ได้** เก็บ analytics event data หรือ user data warehouse ของคุณ raw events ของคุณจะยังคงอยู่ที่เดิม (Snowflake, BigQuery, Redshift ฯลฯ)

export const dockerCmdsCode = `# Start GrowthBook and MongoDB in the background
docker compose up -d

# Stop all containers (data is preserved in the named volumes)
docker compose down

# Tail live logs for the growthbook container
docker compose logs -f growthbook`;

<CopyCode client:visible code={dockerCmdsCode} label="Copy Docker commands" />

<Callout title="Key point">
  Port 3000 คือแอป GrowthBook — admin UI ของคุณ ส่วน Port 3100 คือ SDK API — endpoint ที่แอปของคุณเรียกเพื่อดึง feature flags ทั้งสองมาจากสแตก Compose เดียวกัน
</Callout>

## ข้อแลกเปลี่ยน

| ตัวเลือก | Benefit | Cost |
|---|---|---|
| Self-host ด้วย Docker Compose | ควบคุมได้เต็มที่ ไม่มีค่าใช้จ่าย | ต้องจัดการ ops เอง (backup, upgrade, uptime) |
| GrowthBook Cloud | ไม่ต้องดูแล infrastructure | เสียการควบคุมข้อมูลบางส่วนและอาจมีค่าใช้จ่าย |

## ข้อผิดพลาดที่พบบ่อย

- รัน GrowthBook โดยไม่ persist MongoDB volume ทำให้ data หายตอน restart container
- ไม่ตั้ง environment variable สำคัญ (เช่น `MONGODB_URI`, `JWT_SECRET`) ให้ปลอดภัยสำหรับ production
- ลืม backup database ก่อน upgrade version

> 💡 ตัวอย่างจากของจริง
>
> ทีม startup ขนาดเล็กมักเริ่มด้วย self-host Docker Compose เพื่อประหยัดค่าใช้จ่าย ก่อนย้ายไป managed hosting เมื่อทีมโตขึ้น

export const quizDocker = [
  {
    q: "Port ไหนให้บริการ GrowthBook web UI?",
    options: ["3000", "3100", "8080", "27017"],
    answer: 0,
  },
  {
    q: "SDK ใช้ port ไหนในการดึง feature flags?",
    options: ["3000", "3100", "8080", "27017"],
    answer: 1,
  },
  {
    q: "คำสั่งไหนที่เริ่ม GrowthBook แบบทำงานเบื้องหลัง?",
    options: [
      "docker run growthbook",
      "docker compose up -d",
      "docker start mongo",
      "npm run dev",
    ],
    answer: 1,
  },
  {
    q: "GrowthBook เก็บอะไรไว้ใน MongoDB?",
    options: [
      "analytics event data ของคุณ",
      "config ของ feature, experiments, metrics",
      "Docker images",
      "SDK client keys เท่านั้น",
    ],
    answer: 1,
  },
];

<Quiz client:visible id="intro-setup/run-with-docker" questions={quizDocker} />

<ProgressTracker client:visible id="intro-setup/run-with-docker" />
