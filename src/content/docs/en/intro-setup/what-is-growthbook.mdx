---
title: "What is GrowthBook?"
description: 'An overview of GrowthBook — feature flagging, A/B testing, and warehouse-native analytics.'
sidebar:
  order: 2
---

import CopyCode from '../../../../components/CopyCode.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

GrowthBook is an open-source feature-flagging and experimentation platform built around three core pillars.

## The three pillars

### 1. Feature Flagging

Control feature visibility without requiring a new deploy. Wrap any code path behind a flag, then toggle it on or off — per environment, per audience segment, or for a percentage rollout — straight from the GrowthBook UI.

### 2. A/B Testing / Experimentation

Run controlled experiments to measure the real impact of your changes. GrowthBook assigns users to variants, tracks exposures via your SDK, and then queries your data warehouse to compute statistical results — including Bayesian and frequentist analyses.

### 3. Analytics on your own data warehouse

GrowthBook connects directly to **your** warehouse — Snowflake, BigQuery, Redshift, ClickHouse, and more. It queries your existing event tables and computes metrics on the fly. Your raw event data never leaves your infrastructure.

## Self-host vs GrowthBook Cloud

| | Self-Host | GrowthBook Cloud |
|---|---|---|
| **Cost** | Free | Free tier, then paid |
| **Data control** | Full | Managed |
| **Setup** | Docker Compose | Sign up |
| **URL** | `localhost:3000` | `app.growthbook.io` |

## Key concepts

| Concept | Definition |
|---|---|
| **Feature** | A named flag in GrowthBook that maps to a value in your code. |
| **Environment** | A deployment context — `dev`, `staging`, or `production`. Each feature can have different rules per environment. |
| **SDK Connection** | The pair of `apiHost` + `clientKey` that identifies your app to GrowthBook's CDN. |
| **Experiment** | A controlled A/B or multivariate test linked to one or more Features. |
| **Metric** | A measurable outcome (conversion, revenue, latency) defined against your warehouse tables. |

## Reading a JSON flag

GrowthBook supports more than booleans. Use `getFeatureValue` to read a JSON flag with a typed fallback:

```js
import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-YOUR_DEV_KEY',
});

await gb.init({ timeout: 2000 });

// JSON flag — returns the object or the fallback
const config = gb.getFeatureValue('checkout-config', { maxItems: 10, theme: 'light' });
console.log(config.theme); // 'dark' if the flag is on, 'light' otherwise
```

export const jsonFlagCode = `import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-YOUR_DEV_KEY',
});

await gb.init({ timeout: 2000 });

// JSON flag — returns the object or the fallback
const config = gb.getFeatureValue('checkout-config', { maxItems: 10, theme: 'light' });
console.log(config.theme); // 'dark' if the flag is on, 'light' otherwise`;

<CopyCode client:visible code={jsonFlagCode} label="Copy JSON flag snippet" />

<Callout title="Key point">
GrowthBook runs experiments on YOUR data warehouse — it never ingests or stores your raw event data.
</Callout>

## Common mistakes

- Treating GrowthBook as just an on/off "feature switch" and never using the experimentation/analytics side
- Choosing self-hosting without an ops team to maintain it, leading to frequent downtime
- Not connecting a real data warehouse, so analytics has no data to work with

> 💡 Real-world example
>
> Large consumer products commonly run internal feature-flagging and experimentation systems with similar concepts to test UI changes across tens of millions of users, and organizations with heavy experimentation cultures run large numbers of concurrent experiments using the same core ideas GrowthBook is built on.

export const quizWhatIs = [
  {
    q: 'How many core pillars does GrowthBook have?',
    options: ['1', '3', '5', '7'],
    answer: 1,
  },
  {
    q: 'What does an SDK Connection provide?',
    options: [
      'A username and password',
      'A database URL',
      'An apiHost and clientKey',
      'An experiment ID',
    ],
    answer: 2,
  },
  {
    q: 'Where does GrowthBook store experiment data?',
    options: [
      'In your own data warehouse',
      'On GrowthBook servers',
      'In MongoDB only',
      'In localStorage',
    ],
    answer: 0,
  },
  {
    q: 'What does GrowthBook call a feature flag internally?',
    options: ['Toggle', 'Feature', 'Experiment', 'Segment'],
    answer: 1,
  },
];

<Quiz client:visible id="intro-setup/what-is-growthbook" questions={quizWhatIs} />

<ProgressTracker client:visible id="intro-setup/what-is-growthbook" />
